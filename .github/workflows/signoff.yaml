name: CI
on:
  pull_request:
    types: [opened, reopened, synchronize]
    paths:
      - 'python_workspace/**'
      - '!docs/signoff/**'
jobs:
  check-signoffs:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - run: exit 1